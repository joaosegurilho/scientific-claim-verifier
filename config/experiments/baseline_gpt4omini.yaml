experiment:
  name: "baseline_gpt4omini"
  description: "Default pipeline, no additional features"

model:
  pipeline_model: "gpt4o-mini"
  agent_model: "gpt4o-mini"
  embedding_model: "nomic-embed-text:v1.5"
  llm_temperature: 0
  chunk_size: 300
  chunk_overlap: 50

kb:
  db_name: baseline_gpt4o_kb

features: []

benchmark:
  - dataset: "scifact"
    split: "test"
    max_claims: null
    method: "agentless_with_search"
    max_papers: 10
  
  # - dataset: "msvec"
  #   split: "test"
  #   max_claims: null
  #   method: "agentless_with_search"
  #   max_papers: 10

cost_tracking:
  enabled: true
  budget_usd: 5.0
